plugins {
    id 'com.android.application'
}

android {
    compileSdkVersion rootProject.ext.android.compileSdkVersion
    buildToolsVersion rootProject.ext.android.buildToolsVersion
    namespace = "com.yiman.ad.adbid"
    defaultConfig {
        applicationId "com.limo.adx"
        minSdkVersion rootProject.ext.android.minSdkVersion
        targetSdkVersion rootProject.ext.android.targetSdkVersion
        versionCode 20100019
        versionName "2.1.0.19"
    }

    signingConfigs {
        debug {
            storeFile file("./key/ydyFarm.keystore") // 密钥库文件路径
            storePassword "123456" // 密钥库密码
            keyAlias "aprilIdiom" // 密钥别名
            keyPassword "123456" // 0密钥密码
            enableV2Signing true
        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
            // 是否混淆
            minifyEnabled false
            // 混淆配置文件的位置
        }
        release {
            signingConfig signingConfigs.debug
            minifyEnabled true
            // 混淆配置文件的位置
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_11
        targetCompatibility JavaVersion.VERSION_11
    }
}

repositories {
    flatDir {
        dirs 'libs'
    }
}

dependencies {

    implementation(libs.androidx.core)
    implementation(libs.glide)
    implementation(libs.androidx.recyclerview)
    implementation(libs.play.services.ads.identifier)
    implementation(libs.play.services.location)
    implementation(libs.androidx.appcompat)
    implementation(libs.androidx.constraintlayout)
    implementation 'com.geyifeng.immersionbar:immersionbar:3.2.2'

    //必选：领摩加载库
    implementation(files("libs/adbid_sdk_2.1.0.23_0903_1650.aar"))
    //选择性接入(强烈建议)：微信小程序,有利于提高收益
    implementation("com.tencent.mm.opensdk:wechat-sdk-android-without-mta:6.8.0")

    // 1.必须接入：允许sdk获取oaid,需引入，否则影响收益
    // 2.无需接入：如果禁用sdk获取oaid,必须手动向sdk传入OAID,此时无需接入
    implementation("com.huawei.hms:ads-identifier:3.4.62.300")
    implementation("com.hihonor.mcs:ads-identifier:1.0.2.301")

}

